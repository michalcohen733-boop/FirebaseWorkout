<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FirebaseWorkout.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:FirebaseWorkout.Views"
    Title="FirebaseWorkout">

    <Shell.TitleView>
        <HorizontalStackLayout>

            <!--  HOME Option  -->
            <Button
                Margin="5"
                BackgroundColor="#F9A8D4"
                Command="{Binding NavigateToHomePageCommand}"
                FontFamily="MaterialIcons"
                FontSize="24"
                HorizontalOptions="Start"
                Text="{Binding HomeIcon}"
                TextColor="#831843" />

            <!--  Account Option  -->
            <Button
                Margin="0,5,5,5"
                BackgroundColor="#F9A8D4"
                Command="{Binding NavigateToAccountPageCommand}"
                FontFamily="MaterialIcons"
                FontSize="24"
                HorizontalOptions="Start"
                IsVisible="{Binding ShowAccountAndLogout}"
                Text="{Binding AccountIcon}"
                TextColor="#831843" />


            <!--  IsAdmin Option  -->
            <Button
                Margin="0,5,5,5"
                BackgroundColor="#F9A8D4"
                Command="{Binding NavigateToAdminPageCommand}"
                FontFamily="MaterialIcons"
                FontSize="24"
                HorizontalOptions="Start"
                IsVisible="{Binding IsAdmin}"
                Text="{Binding AdminIcon}"
                TextColor="#831843" />

            <!--  Service Option (Admin or ServicePerson)  -->
            <Button
                Margin="0,5,5,5"
                BackgroundColor="#F9A8D4"
                Command="{Binding NavigateToServicePageCommand}"
                FontFamily="MaterialIcons"
                FontSize="24"
                HorizontalOptions="Start"
                IsVisible="{Binding CanSeeService}"
                Text="{Binding ServiceIcon}"
                TextColor="#831843" />

            <!--  SignOut Option  -->
            <Button
                Margin="0,5,5,5"
                BackgroundColor="#F9A8D4"
                Command="{Binding LogoutCommand}"
                FontFamily="MaterialIcons"
                FontSize="24"
                HorizontalOptions="Start"
                IsVisible="{Binding ShowAccountAndLogout}"
                Text="{Binding LogoutIcon}"
                TextColor="#831843" />


        </HorizontalStackLayout>
    </Shell.TitleView>

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPageView}"
        Route="MainPage" />
</Shell>
