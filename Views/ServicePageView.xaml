<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="FirebaseWorkout.Views.ServicePageView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:FirebaseWorkout.Model"
    Title="Service"
    BackgroundColor="#F9FAFB">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Row 0: Header -->
        <HorizontalStackLayout Grid.Row="0" Padding="20">
            <Label
                Text="Open Issues"
                FontSize="24"
                FontAttributes="Bold"
                TextColor="#1F2937"
                HorizontalOptions="StartAndExpand"
                VerticalOptions="Center" />
            <Label
                Text="{Binding ReportsCount}"
                FontSize="14"
                TextColor="#6B7280"
                VerticalOptions="Center" />
        </HorizontalStackLayout>

        <!-- Row 1: Reports list -->
        <CollectionView
            Grid.Row="1"
            ItemsSource="{Binding Reports}"
            SelectionMode="Single"
            SelectionChanged="OnReportSelected">

            <!-- Empty state -->
            <CollectionView.EmptyView>
                <VerticalStackLayout
                    HorizontalOptions="Center"
                    VerticalOptions="Center"
                    Spacing="8">
                    <Label
                        Text="🎉"
                        FontSize="48"
                        HorizontalOptions="Center" />
                    <Label
                        Text="No open issues"
                        FontSize="18"
                        FontAttributes="Bold"
                        TextColor="#6B7280"
                        HorizontalOptions="Center" />
                    <Label
                        Text="All computers are working well!"
                        FontSize="14"
                        TextColor="#6B7280"
                        HorizontalOptions="Center" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:Report">
                    <Border
                        Margin="20,4"
                        Padding="16"
                        BackgroundColor="White"
                        Stroke="#E5E7EB"
                        StrokeThickness="1"
                        StrokeShape="RoundRectangle 12">
                        <VerticalStackLayout Spacing="8">

                            <!-- Computer number + Status badge -->
                            <HorizontalStackLayout>
                                <Label
                                    Text="{Binding ComputerNumber}"
                                    FontSize="18"
                                    FontAttributes="Bold"
                                    TextColor="#1F2937"
                                    HorizontalOptions="StartAndExpand"
                                    VerticalOptions="Center" />
                                <Border
                                    BackgroundColor="#FEE2E2"
                                    Stroke="Transparent"
                                    StrokeShape="RoundRectangle 8"
                                    Padding="8,4">
                                    <Label
                                        Text="{Binding Status}"
                                        FontSize="12"
                                        FontAttributes="Bold"
                                        TextColor="#EF4444" />
                                </Border>
                            </HorizontalStackLayout>

                            <!-- Room -->
                            <Label
                                Text="{Binding Room}"
                                FontSize="14"
                                TextColor="#6B7280" />

                            <!-- Issues summary -->
                            <Label
                                Text="{Binding IssuesSummary}"
                                FontSize="14"
                                TextColor="#1F2937"
                                LineBreakMode="WordWrap" />

                            <!-- Divider -->
                            <BoxView
                                HeightRequest="1"
                                BackgroundColor="#E5E7EB"
                                Margin="0,4" />

                            <!-- Reporter + Date -->
                            <HorizontalStackLayout>
                                <Label
                                    Text="Reported by:"
                                    FontSize="12"
                                    TextColor="#6B7280" />
                                <Label
                                    Text="{Binding ReporterName}"
                                    FontSize="12"
                                    FontAttributes="Bold"
                                    TextColor="#1F2937"
                                    Margin="4,0,0,0" />
                                <Label
                                    Text="{Binding ReportDate}"
                                    FontSize="12"
                                    TextColor="#6B7280"
                                    HorizontalOptions="EndAndExpand" />
                            </HorizontalStackLayout>

                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>
